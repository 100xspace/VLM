Before attaching adapters, one operational detail matters. Large models are often loaded in reduced precision (8-bit or 4-bit) to fit into GPU memory. When that is the case, the model must be prepared correctly so gradients flow through the adapter weights without destabilizing the frozen quantized backbone. The helper function prepare_model_for_kbit_training() does exactly that. It applies safe defaults such as casting layer norms and enabling gradient checkpointing-friendly settings, which prevents training from silently failing.
